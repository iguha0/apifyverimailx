{
    "title": "Email Verifier Input",
    "description": "Provide a list of email addresses to validate through regex, DNS/MX, and SMTP handshake.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "emails": {
            "title": "Emails to verify",
            "type": "array",
            "description": "Array of email address strings.",
            "editor": "stringList",
            "items": {
                "type": "string"
            },
            "minItems": 1
        }
    },
    "required": ["emails"]
}